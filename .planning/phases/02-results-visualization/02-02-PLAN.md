---
phase: 02-results-visualization
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/results/[id]/page.tsx
  - src/components/results/results-dashboard.tsx
  - src/components/results/results-header.tsx
  - src/components/results/company-card.tsx
  - src/components/results/company-grid.tsx
  - src/components/results/role-list.tsx
  - src/components/results/seniority-dot.tsx
  - src/components/results/empty-state.tsx
  - src/components/__tests__/empty-state.test.tsx
autonomous: true
requirements: [SRCH-02, SRCH-03, SRCH-05, PRIV-01]

must_haves:
  truths:
    - "User sees destination companies as cards with migration counts"
    - "User sees role breakdowns within each company card with seniority dots"
    - "User sees a contextual summary header with stats and New Search button"
    - "User sees a polished empty state with icon, suggestions, and prominent CTA"
    - "No individual names or profiles appear on the page"
  artifacts:
    - path: "src/components/results/results-dashboard.tsx"
      provides: "Client component orchestrating all result views"
      min_lines: 20
    - path: "src/components/results/results-header.tsx"
      provides: "Summary header with stats and New Search button"
      contains: "Where"
    - path: "src/components/results/company-grid.tsx"
      provides: "Responsive grid of company cards"
      min_lines: 10
    - path: "src/components/results/company-card.tsx"
      provides: "Individual company card with role breakdown"
      contains: "Card"
    - path: "src/components/results/role-list.tsx"
      provides: "Expandable role list (top 3, +X more)"
      contains: "expanded"
    - path: "src/components/results/seniority-dot.tsx"
      provides: "Green/amber dot with tooltip"
      contains: "same-or-lower"
    - path: "src/components/results/empty-state.tsx"
      provides: "Polished empty state"
      contains: "Try Another Search"
    - path: "src/app/results/[id]/page.tsx"
      provides: "Server component wiring data to dashboard"
    - path: "src/components/__tests__/empty-state.test.tsx"
      provides: "Render test for empty state component"
  key_links:
    - from: "src/app/results/[id]/page.tsx"
      to: "src/components/results/results-dashboard.tsx"
      via: "Server component passes data as props to client dashboard"
      pattern: "ResultsDashboard"
    - from: "src/components/results/results-dashboard.tsx"
      to: "src/lib/sankey-data.ts"
      via: "Imports groupMigrationsForCards for card data"
      pattern: "groupMigrationsForCards"
    - from: "src/components/results/company-card.tsx"
      to: "src/components/results/role-list.tsx"
      via: "Card renders RoleList with roles array"
      pattern: "RoleList"
---

<objective>
Build the results dashboard UI: summary header, company cards grid with role breakdowns and seniority dots, and polished empty state.

Purpose: This transforms the flat migration list into the grouped, scannable dashboard that is the core user experience of the results page. Users see where people went (grouped by company), what roles they took (expandable list), and relative seniority context (green/amber dots).

Output: Complete results dashboard minus the Sankey visualization (built in Plan 03). The page is fully functional -- Sankey is additive, not blocking.
</objective>

<execution_context>
@/Users/a.ku/.claude/get-shit-done/workflows/execute-plan.md
@/Users/a.ku/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-results-visualization/02-CONTEXT.md
@.planning/phases/02-results-visualization/02-RESEARCH.md
@.planning/phases/02-results-visualization/02-01-SUMMARY.md

<interfaces>
<!-- Types created by Plan 01 that this plan consumes -->

From src/lib/sankey-data.ts (created in Plan 01):
```typescript
export interface CompanyCardData {
  company: string;
  totalCount: number;
  roles: { role: string; count: number; seniority: SeniorityComparison }[];
}
export function groupMigrationsForCards(migrations: MigrationRecord[], searchedRole: string): CompanyCardData[];
```

From src/lib/seniority.ts (created in Plan 01):
```typescript
export type SeniorityComparison = "same-or-lower" | "more-senior";
```

From src/lib/db/schema.ts (updated in Plan 01):
```typescript
// migrations table now includes sourceRole: text("source_role")
```

From existing src/app/results/[id]/page.tsx:
```typescript
// Server component that fetches search + migrations from DB
// Currently renders a flat list -- will be rewritten to use ResultsDashboard
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Results component suite (header, cards, roles, seniority dots, empty state) with empty state test</name>
  <files>src/components/results/results-header.tsx, src/components/results/company-card.tsx, src/components/results/company-grid.tsx, src/components/results/role-list.tsx, src/components/results/seniority-dot.tsx, src/components/results/empty-state.tsx, src/components/__tests__/empty-state.test.tsx</files>
  <action>
    Create the `src/components/results/` directory and build all leaf components:

    **results-header.tsx** (server component, no "use client"):
    - Props: `{ company: string; role: string; totalPeople: number; totalCompanies: number; totalRoles: number }`
    - Heading: "Where [Role]s at [Company] ended up" (per locked decision)
    - Subtitle stats: "X people . Y companies . Z roles" using middle-dot separator
    - "New Search" Button (shadcn) linking to "/" -- prominent, not secondary
    - Layout: flex with heading left, button right on desktop; stacked on mobile

    **seniority-dot.tsx** ("use client" for hover tooltip):
    - Props: `{ level: "same-or-lower" | "more-senior" }`
    - Green dot (bg-emerald-500) for "same-or-lower", amber dot (bg-amber-500) for "more-senior"
    - Size: 8px (size-2 in Tailwind)
    - Tooltip on hover: "Similar level" for green, "Had more experience" for amber
    - Use a simple CSS tooltip (title attribute is acceptable for v1, or a small absolute-positioned span on hover) -- do NOT add a heavy tooltip library

    **role-list.tsx** ("use client" for expand toggle):
    - Props: `{ roles: { role: string; count: number; seniority: SeniorityComparison }[]; maxVisible?: number }`
    - Default maxVisible = 3
    - Each role row: SeniorityDot + role name (left), count badge (right)
    - If roles.length > maxVisible and 5+ total roles: show "+X more roles" button that expands inline (per locked decision: expand for companies with 5+ roles)
    - When expanded, no collapse button needed (one-way expand is fine for v1)

    **company-card.tsx** (server-compatible, wraps client RoleList):
    - Props: `{ data: CompanyCardData }`
    - Uses shadcn Card with CardHeader (company name + total count badge) and CardContent (RoleList)
    - Total count badge: rounded-full bg-primary/10 text-primary, same style as current flat list badges
    - Company name in CardTitle

    **company-grid.tsx** (server component):
    - Props: `{ companies: CompanyCardData[] }`
    - CSS grid: `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` (responsive 1-2-3 columns per locked decision)
    - Gap-4 or gap-6 between cards
    - No pagination, no truncation -- show all companies (per locked decision)

    **empty-state.tsx** (server component):
    - Polish existing empty state from page.tsx into standalone component
    - Add a larger Lucide icon (SearchX or Compass, size-16)
    - Better typography: larger heading (text-2xl), more spacious layout
    - Keep suggestion text from current implementation ("try broader role", "larger company", "different spelling")
    - Prominent "Try Another Search" CTA using shadcn Button (not variant="outline" -- make it primary/default)
    - Props: none needed (or optional company/role for contextual message)

    **src/components/__tests__/empty-state.test.tsx** (component render test):
    - Test that EmptyState renders the "Try Another Search" CTA button
    - Test that suggestion text is present ("try broader role" or similar)
    - Test that the Lucide icon renders (check for SVG element)
    - Use @testing-library/react render + screen queries
    - This satisfies VALIDATION.md Wave 0 requirement for empty-state test coverage
  </action>
  <verify>
    <automated>cd /Users/a.ku/Claude/leavers && npx tsc --noEmit 2>&1 | head -30 && npx vitest run src/components/__tests__/empty-state.test.tsx</automated>
  </verify>
  <done>All 6 component files exist in src/components/results/, empty-state render test passes, TypeScript compiles without errors, components follow locked decisions (card layout, 2-3 col grid, top 3 roles + expand, green/amber dots, polished empty state)</done>
</task>

<task type="auto">
  <name>Task 2: Wire results dashboard and update results page</name>
  <files>src/components/results/results-dashboard.tsx, src/app/results/[id]/page.tsx</files>
  <action>
    **results-dashboard.tsx** ("use client"):
    - Props: `{ search: { company: string; role: string }; migrations: MigrationRecord[] }` where MigrationRecord matches the DB shape (destinationCompany, destinationRole, count, sourceRole?)
    - On mount/render: call `groupMigrationsForCards(migrations, search.role)` to get card data
    - Compute stats: totalPeople (sum of all counts), totalCompanies (unique destination companies), totalRoles (unique destination roles)
    - Render in order:
      1. ResultsHeader (with stats)
      2. Placeholder div for Sankey (Plan 03 will add it here) -- add a comment: `{/* Sankey diagram will be inserted here by Plan 03 */}`
      3. CompanyGrid (with grouped card data)
    - Use useMemo for groupMigrationsForCards call to avoid recomputation

    **Update src/app/results/[id]/page.tsx:**
    - Keep as Server Component for data fetching
    - Remove the current flat list rendering and inline ResultsLayout
    - Keep error state and pending state handling as-is (they work fine)
    - For empty results: render the new EmptyState component instead of inline JSX
    - For results: render ResultsDashboard, passing search record and migration results as props
    - Add a simple layout wrapper (keep the same max-w and padding structure, but widen to max-w-6xl to accommodate the card grid)
    - Keep the "Back to search" link at top
  </action>
  <verify>
    <automated>cd /Users/a.ku/Claude/leavers && npx tsc --noEmit 2>&1 | head -30 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Results page renders grouped company cards with role breakdowns and seniority dots. Empty state shows polished UI with icon and CTA. Page builds without errors. Sankey placeholder is in place for Plan 03.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (no type errors)
- `npm run build` succeeds (SSR/client boundary correct)
- `npx vitest run src/components/__tests__/empty-state.test.tsx` passes
- `npm test` passes (no regressions from Phase 1 tests)
- Results page at /results/[id] shows grouped company cards instead of flat list
- Empty results show polished empty state
</verification>

<success_criteria>
1. Company cards display destination companies grouped with migration counts (SRCH-02)
2. Role breakdowns show within each card with expandable list for 5+ roles (SRCH-03)
3. Seniority dots (green/amber) appear next to roles with tooltip (locked decision)
4. Summary header shows "Where [Role]s at [Company] ended up" with stats (locked decision)
5. Empty state has icon, suggestions, and prominent CTA (SRCH-05)
6. Empty state render test passes
7. No individual names or profiles visible anywhere (PRIV-01)
8. Build succeeds, no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-results-visualization/02-02-SUMMARY.md`
</output>
