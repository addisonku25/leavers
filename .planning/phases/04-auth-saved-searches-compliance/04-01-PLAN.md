---
phase: 04-auth-saved-searches-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema.ts
  - src/lib/auth.ts
  - src/lib/auth-client.ts
  - src/app/api/auth/[...all]/route.ts
  - src/app/login/page.tsx
  - src/app/signup/page.tsx
  - src/components/nav-bar.tsx
  - src/components/auth/login-form.tsx
  - src/components/auth/signup-form.tsx
  - src/app/layout.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "User can sign up with email and password and is auto-signed-in"
    - "User can log in with email and password"
    - "User can log out from any page via the nav bar"
    - "Session persists across browser sessions (cookie-based)"
    - "Nav bar shows Sign In / Sign Up for guests, email + logout for authenticated users"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "Better Auth tables (user, session, account, verification) + savedSearches table"
      contains: "sqliteTable"
    - path: "src/lib/auth.ts"
      provides: "Better Auth server instance"
      exports: ["auth"]
    - path: "src/lib/auth-client.ts"
      provides: "Better Auth client hooks"
      exports: ["authClient"]
    - path: "src/app/api/auth/[...all]/route.ts"
      provides: "Better Auth route handler"
      exports: ["GET", "POST"]
    - path: "src/app/login/page.tsx"
      provides: "Login page"
    - path: "src/app/signup/page.tsx"
      provides: "Signup page with returnTo support"
    - path: "src/components/nav-bar.tsx"
      provides: "Persistent nav bar with auth state"
    - path: "src/components/auth/login-form.tsx"
      provides: "Login form component"
    - path: "src/components/auth/signup-form.tsx"
      provides: "Signup form component"
  key_links:
    - from: "src/components/auth/login-form.tsx"
      to: "src/lib/auth-client.ts"
      via: "authClient.signIn.email()"
      pattern: "authClient\\.signIn"
    - from: "src/components/auth/signup-form.tsx"
      to: "src/lib/auth-client.ts"
      via: "authClient.signUp.email()"
      pattern: "authClient\\.signUp"
    - from: "src/components/nav-bar.tsx"
      to: "src/lib/auth-client.ts"
      via: "authClient.useSession() for reactive state"
      pattern: "useSession"
    - from: "src/app/api/auth/[...all]/route.ts"
      to: "src/lib/auth.ts"
      via: "toNextJsHandler(auth)"
      pattern: "toNextJsHandler"
    - from: "src/lib/auth.ts"
      to: "src/lib/db/index.ts"
      via: "drizzleAdapter(db)"
      pattern: "drizzleAdapter"
---

<objective>
Set up Better Auth with email/password authentication, create login and signup pages, and add a persistent nav bar with auth state across all pages.

Purpose: Users need accounts to save searches (Phase 4 core). Auth infrastructure must exist before saved searches or rate limiting can differentiate between authenticated and guest users.
Output: Working signup/login/logout flow with persistent nav bar showing auth state.
</objective>

<execution_context>
@/Users/a.ku/.claude/get-shit-done/workflows/execute-plan.md
@/Users/a.ku/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-auth-saved-searches-compliance/04-RESEARCH.md
@.planning/phases/04-auth-saved-searches-compliance/04-CONTEXT.md

<interfaces>
<!-- Existing schema that auth tables must merge into -->
From src/lib/db/schema.ts:
```typescript
import { integer, sqliteTable, text } from "drizzle-orm/sqlite-core";
export const searches = sqliteTable("searches", { ... });
export const migrations = sqliteTable("migrations", { ... });
```

From src/lib/db/index.ts:
```typescript
import { createClient } from "@libsql/client";
import { drizzle } from "drizzle-orm/libsql";
import * as schema from "./schema";
const client = createClient({ url: process.env.TURSO_DATABASE_URL ?? "file:local.db", authToken: process.env.TURSO_AUTH_TOKEN });
export const db = drizzle(client, { schema });
```

From src/app/layout.tsx:
```typescript
export default function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>{children}</body>
    </html>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database schema + Better Auth server/client config</name>
  <files>src/lib/db/schema.ts, src/lib/auth.ts, src/lib/auth-client.ts, src/app/api/auth/[...all]/route.ts</files>
  <action>
1. Install dependencies: `npm install better-auth @upstash/ratelimit`

2. Update `src/lib/db/schema.ts` -- MERGE Better Auth tables into the existing file alongside `searches` and `migrations` tables. Add these tables:
   - `user` table: id (text PK), name (text), email (text, unique), emailVerified (integer boolean, default false), image (text nullable), createdAt (integer timestamp), updatedAt (integer timestamp)
   - `session` table: id (text PK), expiresAt (integer timestamp), token (text, unique), ipAddress (text nullable), userAgent (text nullable), userId (text FK -> user.id), createdAt, updatedAt
   - `account` table: id (text PK), accountId (text), providerId (text), userId (text FK -> user.id), accessToken (text nullable), refreshToken (text nullable), idToken (text nullable), accessTokenExpiresAt (integer timestamp nullable), refreshTokenExpiresAt (integer timestamp nullable), scope (text nullable), password (text nullable), createdAt, updatedAt
   - `verification` table: id (text PK), identifier (text), value (text), expiresAt (integer timestamp), createdAt (integer timestamp nullable), updatedAt (integer timestamp nullable)
   - `savedSearches` table: id (text PK), userId (text FK -> user.id), searchId (text FK -> searches.id), createdAt (integer timestamp)

   Run `npx @better-auth/cli generate` first to verify exact column names, then merge into schema.ts. Keep all tables in one file.

3. Create `src/lib/auth.ts` -- Better Auth server instance:
   - Import `betterAuth` from "better-auth", `drizzleAdapter` from "better-auth/adapters/drizzle", `nextCookies` from "better-auth/next-js"
   - Import `db` from "./db"
   - Configure: `database: drizzleAdapter(db, { provider: "sqlite" })`, `emailAndPassword: { enabled: true, autoSignIn: true }`, `plugins: [nextCookies()]`
   - Export `auth`

4. Create `src/lib/auth-client.ts` -- Client-side auth:
   - Import `createAuthClient` from "better-auth/react"
   - Export `authClient = createAuthClient()`

5. Create `src/app/api/auth/[...all]/route.ts` -- Route handler:
   - Import `auth` from "@/lib/auth", `toNextJsHandler` from "better-auth/next-js"
   - Export `const { GET, POST } = toNextJsHandler(auth)`

6. Push schema: `npx drizzle-kit push`
  </action>
  <verify>
    <automated>cd /Users/a.ku/Claude/leavers && npx drizzle-kit push --force && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Better Auth tables exist in schema.ts alongside existing tables. Auth server instance configured with Drizzle adapter. Route handler exports GET and POST. Schema pushed to database. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Login and signup pages with auth forms</name>
  <files>src/app/login/page.tsx, src/app/signup/page.tsx, src/components/auth/login-form.tsx, src/components/auth/signup-form.tsx</files>
  <action>
1. Create `src/components/auth/login-form.tsx` ("use client"):
   - Use react-hook-form + zod validation (same pattern as search-form.tsx)
   - Schema: email (string, email format), password (string, min 8 chars)
   - On submit: call `authClient.signIn.email({ email, password })` from auth-client.ts
   - On success: redirect to "/" using `router.push("/")`
   - On error: show inline error message ("Invalid email or password")
   - Show loading state on submit button
   - Use shadcn Input, Button, Card components
   - Include "Don't have an account? Sign up" link to /signup

2. Create `src/components/auth/signup-form.tsx` ("use client"):
   - Same form pattern. Schema: name (string, min 2), email (string, email), password (string, min 8), confirmPassword (string, must match password)
   - On submit: call `authClient.signUp.email({ email, password, name })`
   - Support `returnTo` and `autoSave` query params: read from `useSearchParams()`. After successful signup, redirect to `returnTo` URL (with autoSave param preserved) or "/" if no returnTo
   - On error: show inline error (e.g., "Email already in use")
   - Include "Already have an account? Sign in" link to /login

3. Create `src/app/login/page.tsx`:
   - Server component wrapper. Centered layout (similar to landing page). Render LoginForm.
   - Page title: "Sign In"

4. Create `src/app/signup/page.tsx`:
   - Server component wrapper. Centered layout. Render SignupForm.
   - Pass through searchParams to support returnTo flow
   - Page title: "Create Account"
  </action>
  <verify>
    <automated>cd /Users/a.ku/Claude/leavers && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Login page at /login renders email+password form. Signup page at /signup renders name+email+password+confirm form. Both use shadcn components and react-hook-form+zod validation. Signup supports returnTo query param for save-search redirect flow. autoSignIn after signup works via Better Auth config.</done>
</task>

<task type="auto">
  <name>Task 3: Persistent nav bar with auth state + layout wiring</name>
  <files>src/components/nav-bar.tsx, src/app/layout.tsx</files>
  <action>
1. Create `src/components/nav-bar.tsx` ("use client"):
   - Uses `authClient.useSession()` for reactive auth state
   - Guest state: Logo/app name "Leavers" on left (links to /), "Sign In" and "Sign Up" buttons on right
   - Authenticated state: Logo on left, user email display + "Saved" link (to /saved) + "Sign Out" button on right
   - Sign Out: calls `authClient.signOut()` then `router.push("/")`
   - Responsive: hamburger menu or simplified layout on mobile (Claude's discretion on specific approach)
   - Minimal styling -- should not compete with search hero on landing page. Use subtle border-bottom, py-3, max-w-6xl centered
   - Show skeleton/loading state while session is pending (isPending from useSession)

2. Update `src/app/layout.tsx`:
   - Add NavBar component above {children} inside the body
   - Nav bar goes inside the body, above children, so it appears on every page
   - Do NOT wrap in a session provider -- Better Auth's useSession works without one
   - Keep existing font and metadata configuration
  </action>
  <verify>
    <automated>cd /Users/a.ku/Claude/leavers && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Nav bar appears on every page. Shows Sign In/Sign Up for guests. Shows email, Saved link, and Sign Out for authenticated users. Layout.tsx updated with NavBar above children. Build succeeds with no errors.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no errors
- `npm run test` passes (existing tests still green)
- Auth route handler responds at /api/auth/* paths
- Login and signup pages render at /login and /signup
- Nav bar renders on all pages
</verification>

<success_criteria>
- User can visit /signup, create an account with email+password, and be auto-signed in
- User can visit /login, sign in with existing credentials
- Nav bar updates reactively to show authenticated state after login
- User can click Sign Out in nav bar and be returned to guest state
- Session cookie persists across browser tab closes (AUTH-02)
- Signup page accepts returnTo query param for redirect after signup
</success_criteria>

<output>
After completion, create `.planning/phases/04-auth-saved-searches-compliance/04-01-SUMMARY.md`
</output>
