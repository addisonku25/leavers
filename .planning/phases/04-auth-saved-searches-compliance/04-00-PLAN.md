---
phase: 04-auth-saved-searches-compliance
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/__tests__/auth.test.ts
  - src/actions/__tests__/saved-searches.test.ts
  - src/lib/__tests__/rate-limit.test.ts
  - src/app/__tests__/legal-pages.test.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-03
  - SAVE-01
  - SAVE-02
  - SAVE-03
  - PRIV-02
  - PRIV-03

must_haves:
  truths:
    - "Test stubs exist for all automated verification targets before implementation begins"
    - "Running npm run test shows the stub files are recognized by Vitest (they can skip or fail, but must be loadable)"
  artifacts:
    - path: "src/lib/__tests__/auth.test.ts"
      provides: "Test stubs for AUTH-01 (signup) and AUTH-03 (signout)"
    - path: "src/actions/__tests__/saved-searches.test.ts"
      provides: "Test stubs for SAVE-01 (save), SAVE-02 (list), SAVE-03 (delete)"
    - path: "src/lib/__tests__/rate-limit.test.ts"
      provides: "Test stubs for PRIV-02 (rate limiting)"
    - path: "src/app/__tests__/legal-pages.test.tsx"
      provides: "Test stubs for PRIV-03 (legal pages render)"
  key_links: []
---

<objective>
Create test stub files for all Phase 4 automated verification targets, establishing the Nyquist contract before implementation begins.

Purpose: VALIDATION.md requires 4 test files as Wave 0 dependencies. These stubs define expected behaviors that Plans 01-04 will make pass during implementation.
Output: 4 test stub files with skipped/todo tests describing expected behaviors.
</objective>

<execution_context>
@/Users/a.ku/.claude/get-shit-done/workflows/execute-plan.md
@/Users/a.ku/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-auth-saved-searches-compliance/04-VALIDATION.md
@.planning/phases/04-auth-saved-searches-compliance/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auth and rate-limit test stubs</name>
  <files>src/lib/__tests__/auth.test.ts, src/lib/__tests__/rate-limit.test.ts</files>
  <action>
1. Create `src/lib/__tests__/auth.test.ts`:
   - Import `describe`, `it`, `expect` from "vitest"
   - `describe("auth")` with:
     - `it.todo("signup - creates user with email and password")` — AUTH-01
     - `it.todo("signup - auto-signs-in after account creation")` — AUTH-01
     - `it.todo("signin - authenticates existing user with correct credentials")` — AUTH-01
     - `it.todo("signin - rejects invalid credentials")` — AUTH-01
     - `it.todo("signout - clears session")` — AUTH-03
   - Use `it.todo()` (Vitest built-in) so tests are recognized but skipped

2. Create `src/lib/__tests__/rate-limit.test.ts`:
   - Import `describe`, `it`, `expect` from "vitest"
   - `describe("rate-limit")` with:
     - `it.todo("creates guest search limiter with 3/day window")` — PRIV-02
     - `it.todo("creates auth search limiter with 50/hour window")` — PRIV-02
     - `it.todo("creates auth endpoint limiter with 5/15min window")` — PRIV-02
     - `it.todo("exports null limiters when redis is unavailable")` — PRIV-02
   - Use `it.todo()` for all tests

Ensure __tests__ directories are created (mkdir -p if needed).
  </action>
  <verify>
    <automated>cd /Users/a.ku/Claude/leavers && npx vitest run src/lib/__tests__/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files exist under src/lib/__tests__/, are recognized by Vitest, and show as skipped/todo in test output.</done>
</task>

<task type="auto">
  <name>Task 2: Saved searches and legal pages test stubs</name>
  <files>src/actions/__tests__/saved-searches.test.ts, src/app/__tests__/legal-pages.test.tsx</files>
  <action>
1. Create `src/actions/__tests__/saved-searches.test.ts`:
   - Import `describe`, `it`, `expect` from "vitest"
   - `describe("saved-searches")` with:
     - `it.todo("save - saves search for authenticated user")` — SAVE-01
     - `it.todo("save - is idempotent for same user+search")` — SAVE-01
     - `it.todo("save - rejects unauthenticated requests")` — SAVE-01
     - `it.todo("list - returns saved searches for authenticated user")` — SAVE-02
     - `it.todo("list - orders by createdAt descending")` — SAVE-02
     - `it.todo("delete - removes saved search owned by user")` — SAVE-03
     - `it.todo("delete - rejects deletion of another user's search")` — SAVE-03
   - Use `it.todo()` for all tests

2. Create `src/app/__tests__/legal-pages.test.tsx`:
   - Import `describe`, `it`, `expect` from "vitest"
   - `describe("legal-pages")` with:
     - `it.todo("terms page renders without error")` — PRIV-03
     - `it.todo("privacy page renders without error")` — PRIV-03
     - `it.todo("privacy page does not mention LinkedIn")` — PRIV-03
   - Use `it.todo()` for all tests

Ensure __tests__ directories are created (mkdir -p if needed).
  </action>
  <verify>
    <automated>cd /Users/a.ku/Claude/leavers && npx vitest run src/actions/__tests__/ src/app/__tests__/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files exist under their respective __tests__/ directories, are recognized by Vitest, and show as skipped/todo in test output.</done>
</task>

</tasks>

<verification>
- `npm run test` passes (all stubs are todo/skipped, no failures)
- All 4 test files exist at their expected paths
- Vitest recognizes all test files in verbose output
</verification>

<success_criteria>
- 4 test stub files created matching VALIDATION.md Wave 0 requirements
- All tests use `it.todo()` so they are skipped, not failing
- `npm run test` remains green (existing tests pass, stubs skip)
</success_criteria>

<output>
After completion, create `.planning/phases/04-auth-saved-searches-compliance/04-00-SUMMARY.md`
</output>
