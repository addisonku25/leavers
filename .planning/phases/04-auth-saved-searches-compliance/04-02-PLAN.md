---
phase: 04-auth-saved-searches-compliance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/terms/page.tsx
  - src/app/privacy/page.tsx
  - src/components/footer.tsx
  - src/app/layout.tsx
autonomous: true
requirements:
  - PRIV-03

must_haves:
  truths:
    - "User can navigate to /terms and see terms of service content"
    - "User can navigate to /privacy and see privacy policy content"
    - "Footer with legal links appears on every page"
    - "Privacy policy references 'publicly available professional data' without naming LinkedIn"
  artifacts:
    - path: "src/app/terms/page.tsx"
      provides: "Terms of service page"
    - path: "src/app/privacy/page.tsx"
      provides: "Privacy policy page"
    - path: "src/components/footer.tsx"
      provides: "Site-wide footer component"
  key_links:
    - from: "src/components/footer.tsx"
      to: "/terms"
      via: "Next.js Link component"
      pattern: "href.*terms"
    - from: "src/components/footer.tsx"
      to: "/privacy"
      via: "Next.js Link component"
      pattern: "href.*privacy"
    - from: "src/app/layout.tsx"
      to: "src/components/footer.tsx"
      via: "Footer component rendered below children"
      pattern: "<Footer"
---

<objective>
Create terms of service and privacy policy pages with a persistent site-wide footer linking to them.

Purpose: Legal compliance for a product that uses publicly available professional data. These are checkbox requirements for v1 -- minimal, honest, template-based content.
Output: Two legal pages and a footer visible on every page.
</objective>

<execution_context>
@/Users/a.ku/.claude/get-shit-done/workflows/execute-plan.md
@/Users/a.ku/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-auth-saved-searches-compliance/04-CONTEXT.md

<interfaces>
<!-- Layout will be modified by both Plan 01 and Plan 02. Plan 02 adds footer. -->
<!-- NOTE: Plan 01 adds NavBar to layout. Plan 02 adds Footer. Since both modify layout.tsx,
     the executor should read the current state of layout.tsx before modifying it. -->
From src/app/layout.tsx:
```typescript
export default function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>{children}</body>
    </html>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Terms of service and privacy policy pages</name>
  <files>src/app/terms/page.tsx, src/app/privacy/page.tsx</files>
  <action>
1. Create `src/app/terms/page.tsx` -- Static server component:
   - Page title in metadata: "Terms of Service | Leavers"
   - Centered prose layout (max-w-3xl, py-16, px-4)
   - Content sections (template-based, not lawyer-reviewed):
     - "Terms of Service" heading with "Last updated: [current date]"
     - Acceptance of Terms: By using Leavers you agree to these terms
     - Description of Service: Career migration intelligence using publicly available professional data
     - User Accounts: Account creation, responsibility for credentials, right to terminate accounts
     - Acceptable Use: No scraping, no automated access, no redistribution
     - Data Accuracy: Information is aggregated and approximate, not guaranteed accurate
     - Limitation of Liability: Standard "as-is" disclaimer, no warranties
     - Changes to Terms: We may update these terms, continued use = acceptance
   - Use Tailwind prose-like styling (headings, paragraphs, lists)

2. Create `src/app/privacy/page.tsx` -- Static server component:
   - Page title in metadata: "Privacy Policy | Leavers"
   - Same centered layout as terms page
   - Content sections:
     - "Privacy Policy" heading with "Last updated: [current date]"
     - Information We Collect: Email address and password (for accounts), search queries, usage data
     - How We Use Data: To provide career migration insights, to save searches for registered users
     - Data Sources: "We analyze publicly available professional data to generate aggregated career migration patterns." DO NOT name LinkedIn or any specific source.
     - Data Aggregation: All displayed results are aggregated and anonymized. No individual profiles are shown.
     - Cookies: Session cookies for authentication
     - Data Retention: Account data retained until deletion, search cache expires after 30 days
     - Your Rights: Users can delete their account and all associated data
     - Contact: Placeholder contact email
   - Same Tailwind prose styling
  </action>
  <verify>
    <automated>cd /Users/a.ku/Claude/leavers && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>/terms and /privacy pages render with complete legal content. Privacy policy says "publicly available professional data" and does not mention LinkedIn. Both pages use consistent prose layout.</done>
</task>

<task type="auto">
  <name>Task 2: Footer component + layout integration</name>
  <files>src/components/footer.tsx, src/app/layout.tsx</files>
  <action>
1. Create `src/components/footer.tsx`:
   - Server component (no client-side state needed)
   - Centered layout matching nav bar width (max-w-6xl)
   - Content: "Leavers" or copyright on left, "Terms of Service" and "Privacy Policy" links on right
   - Links use Next.js Link component to /terms and /privacy
   - Subtle top border, muted text color, py-6, small text (text-sm)
   - Responsive: stack vertically on mobile if needed

2. Update `src/app/layout.tsx`:
   - IMPORTANT: Read the current file first -- Plan 01 may have already added NavBar.
   - Add Footer component below {children} inside the body
   - Structure should be: NavBar (from Plan 01) -> main content (children) -> Footer
   - Wrap children in a flex-grow container so footer stays at page bottom:
     ```
     <body className="... flex min-h-screen flex-col">
       <NavBar />
       <main className="flex-1">{children}</main>
       <Footer />
     </body>
     ```
   - If NavBar is not yet in layout (Plan 01 hasn't run), just add Footer below children. The flex layout will accommodate NavBar when Plan 01 adds it.
  </action>
  <verify>
    <automated>cd /Users/a.ku/Claude/leavers && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Footer appears on every page with links to /terms and /privacy. Footer stays at bottom of page. Layout uses flex column for proper spacing.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- /terms route renders terms of service content
- /privacy route renders privacy policy content
- Footer visible on landing page, results page, and legal pages
- Privacy policy does NOT contain the word "LinkedIn"
</verification>

<success_criteria>
- Both legal pages accessible at their routes with complete content
- Footer links navigate to the correct pages
- Footer appears consistently on all pages
- Privacy policy references "publicly available professional data" without naming sources
</success_criteria>

<output>
After completion, create `.planning/phases/04-auth-saved-searches-compliance/04-02-SUMMARY.md`
</output>
